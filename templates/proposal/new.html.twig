{% extends 'base.html.twig' %}

{% block title %}Nouvelle vision{% endblock %}

{% block body %}
    <div class="container my-5">
        <div class="row">
            <div class="col">
                <h1>Nouvelle vision</h1>
                <div class="my-3">
                    <a href="{{ path('home') }}">Retour à la liste</a>
                </div>

                {{ include('proposal/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
